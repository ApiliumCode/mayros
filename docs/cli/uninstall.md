---
summary: "CLI reference for `mayros uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `mayros uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
mayros uninstall
mayros uninstall --all --yes
mayros uninstall --dry-run
```
